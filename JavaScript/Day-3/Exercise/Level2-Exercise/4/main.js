//declaring constants, And inputting values.
const PI = Math.PI;

const r = parseFloat(prompt("Enter The Value OF The Radius: ",''));

// performing the calculations.

const area = PI * r * r;

const c = 2 * PI * r;

//showing the outputs
alert(`Area Of Circle is ${area}`);
alert(`Circumpherence Of Circle is ${c}`);

console.log(`Area Of Circle is ${area}`);
console.log(`Circumference Of Circle is ${c}`);